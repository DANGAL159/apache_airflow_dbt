version: 2

models:
  - name: int_ventas_enriquecidas
    description: Integra ventas con clientes, productos y sucursales para simplificar la construccion de marts.
    columns:
      - name: venta_id
        tests:
          - not_null
          - unique
      - name: margen_estimado
        description: Monto neto menos costo estimado de los productos vendidos.

